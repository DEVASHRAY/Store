import React from "react";

const BackBtn = () => {

/** Component returns backbutton and takes to the landing page when clicked  */

  return (
    <div className="nodata">
      <button className="nodata-btn">
        <a href="./GetData.js">Go Back</a>
      </button>
    </div>
  );
};

export default BackBtn;
